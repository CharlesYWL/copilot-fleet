{
  "name": "copilot-fleet",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run build -w @fleet/protocol && concurrently -n host,node \"npm run host\" \"npm run node\"",
    "dev:tunnel": "npm run build -w @fleet/protocol && concurrently -n tunnel,host,node \"npm run tunnel\" \"npm run host\" \"npm run node\"",
    "tunnel": "npm run tunnel -w @fleet/host",
    "host": "npm run dev -w @fleet/host",
    "node": "npm run dev -w @fleet/node",
    "build": "npm run build -w @fleet/protocol && npm run build -w @fleet/host && npm run build -w @fleet/node",
    "build:node": "npm run build -w @fleet/protocol && npm run build -w @fleet/node",
    "start": "concurrently -n host,node \"npm run start:host\" \"npm run start:node\"",
    "start:host": "NODE_ENV=production npm run start -w @fleet/host",
    "start:node": "npm run start -w @fleet/node",
    "test": "npm run build -w @fleet/protocol && vitest run",
    "typecheck": "npm run build -w @fleet/protocol && npm run typecheck -w @fleet/host && npm run typecheck -w @fleet/node && npm run typecheck -w @fleet/protocol",
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "engines": {
    "node": ">=22.5"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@testing-library/dom": "^10.4.1",
    "@testing-library/react": "^16.3.2",
    "@types/node": "^24.0.0",
    "@vitest/coverage-v8": "^3.2.7",
    "concurrently": "^9.2.1",
    "eslint": "^10.8.1",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-react-hooks": "^7.1.1",
    "globals": "^17.9.0",
    "jsdom": "^30.0.1",
    "prettier": "^3.9.6",
    "typescript": "^5.9.2",
    "typescript-eslint": "^8.66.0",
    "vitest": "^3.2.4"
  }
}
